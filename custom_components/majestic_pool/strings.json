{
  "config": {
    "step": {
      "user": {
        "title": "Majestic Pool",
        "description": "Configure your Majestic pool controller over BLE.",
        "data": {
          "name": "Name",
          "address": "BLE address",
          "poll_interval": "Poll interval (seconds)",
          "temperature_command": "Temperature command byte",
          "action_commands": "Actions (Label:cmd_hex[:payload_hex], ...)",
          "diagnostic_commands": "Diagnostic poll commands (hex list)",
          "enable_temperature_poll": "Enable periodic temperature polling",
          "connect_on_demand": "Connect BLE only for action/poll",
          "switch_definitions": "Switches (Label|on_cmd|on_payload|off_cmd|off_payload|state_cmd|on_value)",
          "value_sensor_definitions": "Value sensors (Label|cmd|byte_index|scale)"
        }
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Majestic Pool options",
        "data": {
          "poll_interval": "Poll interval (seconds)",
          "temperature_command": "Temperature command byte",
          "action_commands": "Actions (Label:cmd_hex[:payload_hex], ...)",
          "diagnostic_commands": "Diagnostic poll commands (hex list)",
          "enable_temperature_poll": "Enable periodic temperature polling",
          "connect_on_demand": "Connect BLE only for action/poll",
          "switch_definitions": "Switches (Label|on_cmd|on_payload|off_cmd|off_payload|state_cmd|on_value)",
          "value_sensor_definitions": "Value sensors (Label|cmd|byte_index|scale)"
        }
      }
    }
  }
}
