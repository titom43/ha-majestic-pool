{
  "config": {
    "step": {
      "user": {
        "title": "Majestic Pool",
        "description": "Quick setup: put the controller in pairing mode, then select a detected device or use fallback prefix discovery. {ble_hint}",
        "data": {
          "name": "Name",
          "discovered_device": "Detected controller (recommended)",
          "address": "Manual BLE address (optional)",
          "device_name_prefix": "Fallback BLE name prefix (e.g. KKTO_)",
          "poll_interval": "Refresh interval (seconds)"
        }
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Majestic Pool options",
        "data": {
          "poll_interval": "Refresh interval (seconds)",
          "temperature_command": "Temperature command byte",
          "action_commands": "Actions (Label:cmd_hex[:payload_hex], ...)",
          "diagnostic_commands": "Diagnostic commands (hex list)",
          "enable_temperature_poll": "Enable periodic polling",
          "connect_on_demand": "Connect on demand",
          "enable_pairing_probe": "Validate pairing before send",
          "pairing_timeout": "Pairing validation timeout (seconds)",
          "device_name_prefix": "Fallback BLE name prefix (e.g. KKTO_)",
          "switch_definitions": "Switches (Label|on_cmd|on_payload|off_cmd|off_payload|state_cmd|on_value)",
          "value_sensor_definitions": "Value sensors (Label|cmd|byte_index|scale)"
        }
      }
    }
  }
}
